package main

/*
计数质数
统计所有小于非负整数 n 的质数的数量。
2, 3, 5,6, 7, 11, 12, 13, 17,18, 19, 23, 24
*/

func countPrimes(n int) int {

	//i的
	for i := 6; i < n; i += 6 {
		// 判断i-1和i+1是否是质数
		// 是count++
	}
}

func isPrimes(n int) bool {
	// 使用n除以，小于n的平方根的数，都不能整除，返回是，否则返回是

}
